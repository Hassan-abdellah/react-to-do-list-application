import React from 'react'

const Header = () => {
    return (
        <header>
            <h1>Todo List</h1>
            <p>Here is simple Todo-List App you can Add, remove Todo and also check it when completed</p>
        </header>
    )
}

export default Header;
